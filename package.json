{
  "name": "social-media-app",
  "version": "1.0.0",
  "description": "Full-stack social media application with React frontend and Node.js backend",
  "main": "backend/server.js",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "vercel-build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm start",
    "start:prod": "NODE_ENV=production cd backend && npm start",
    "deploy": "npm run build && npm run start:prod",
    "test": "echo \"No tests specified\"",
    "lint": "cd frontend && npm run lint"
  },
  "keywords": ["social-media", "react", "nodejs", "mongodb", "fullstack"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
